<div class="row bg-body-secondary">
    <div class="col">
        @ViewBag.course.Name
    </div>
    <div class="col">
        <a asp-action="Details" asp-route-id="@ViewBag.group.Id" asp-route-courseid="@ViewBag.course.Id" asp-route-typ="show" class="btn btn-primary btn-sm">Listy obecności</a>
    </div>
    <div class="col">
        
    </div>
</div>

